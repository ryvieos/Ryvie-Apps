services:
  mealie:
    container_name: app-mealie
    image: hkotel/mealie:v3.14.0
    volumes:
      - ./mealie-data:/app/data/
    ports:
      - 9091:9000
    environment:
      TZ: Europe/Paris
    restart: unless-stopped
    
volumes:
  mealie-data:
   